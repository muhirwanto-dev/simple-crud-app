namespace simplecrudapp
{
    /// <summary>
    /// Enum for CRUD command.
    /// </summary>
    public enum ECommand
    {
        Unknown = -1,
        Create,
        Read,
        Update,
        Delete,
    }
}
